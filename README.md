> ## ⚠️ 封号风险提醒（务必先读）
>
> **最近封号非常严重，请谨慎使用任何协议类工具。**
>
> 市面上现存的协议（包括本项目在内）目前都存在封号情况，**我自己也已经进去好几个号了**。
>
> 说句实话：这类协议已经没什么继续更新的必要了 —— 只要还在用，迟早会封。
>
> 还是建议大家回归手动操作吧，账号安全比省下来的那点时间重要得多。
>
> 本项目会继续开源保留，但**请自行评估风险后再决定是否使用，使用即代表你已知晓并接受相应后果**。

> ⭐ **开源不易，请动动小手给个 [Star](https://github.com/Aoluis1005/QQ-farm-BOT-GO) 吧！**

# 🎉 QQ 农场 Bot（Go）v1.0.1

> **正式版 v1.0.1**——遇到问题请 [提 Issue](https://github.com/Aoluis1005/QQ-farm-BOT-GO/issues)，我会尽快修复。
>
> 活动中心按游戏 `List` 接口的进行中时间窗动态展示。已结束的鹊桥寄情 / 雨落成诗 / 青梅酿万金 / 公益小红花不再硬塞进面板。
>
> 背包里 `ItemInfo` 有记录、但 `Plant.json` 缺映射的种子会按 1x1 兜底参与自动种植。

QQ 农场微信小游戏协议自动化挂机 Bot——**Go 单文件二进制版**，免 Node 环境，开箱即用。

## ✨ 功能特性

### 🌾 农场自动化
- 自动种植（多种策略：背包优先 / 最高等级 / 最大经验时 / 最大普肥经验时 / 收益最优）
- 自动收获 + 自动卖果实（分批出售，跳过不可售物品）
- 智能施肥（多季 / 快成熟 / 自动购买化肥）
- 除草 / 除虫 / 浇水 / 一键务农
- 土地升级 / 解锁

### 👥 好友自动化
- 巡查偷菜（支持偷菜黑名单 / 白名单）
- 帮忙务农（浇水 / 除草 / 除虫，每日经验上限自动停止）
- 放虫 / 放草 / 捉虫
- 护主犬同气礼包自动领取
- 好友农场拜访（Enter / Leave）

### 🔍 好友检测（疑似外挂识别）
> 基于访客行为统计的**纯客户端分析**，不上传任何数据，让"挂机者"无所遁形。

- **嫌疑画像**：从访客互动记录聚合出行为指纹——操作间隔规律性 / 24h 在线 / 凌晨活跃占比 / 日均操作频率 / 行为单一性，综合评出 0-100 嫌疑分（高 / 中 / 低 / 正常）
- **双价值分体系**：为每个好友算两套独立分数
  - **帮价值**（驱动帮忙排序）：护主犬 +38 · 帮回报 ×3 · 活跃度 ×2 − 被偷 ×2 − 被捣 ×5 − 风险扣分
  - **偷价值**（驱动偷菜排序）：我偷 TA ×4 + 实时可偷地块数 ×10，偷菜为单向行为不受嫌疑分影响
- **自动化联动**：帮忙目标按帮价值降序抢占名额，低价值好友自动降频（每 3 轮参与一次）；偷菜目标按偷价值降序，菜多 + 产出高 + 不反偷的优先
- **好友全集覆盖**：评估集合 = 全部好友（不只访客），零互动好友也有静态价值分，不会被误降级
- **六维筛选**：全部 / 帮价值高 / 偷价值高 / 🔴嫌疑高 / 低帮价值 / 建议清洗，一键切换视角
- **一键清洗**：按嫌疑档位批量删除疑似外挂好友（支持排除护主犬 + 管理员密码确认，删除不可恢复）
- **数据持久化**：偷菜埋点落盘（`stealTo.json`），升级重启不丢失

### 🎯 活动自动化
- 每日任务自动领取（成长任务 / 每日任务 / 主线任务）
- 活跃度奖励自动领取
- 图鉴一键购 + 点券奖励自动领取
- **商城免费礼包** + **每日分享礼包**自动领取
- 进行中活动奖励自动领取（千星游记战令 / 节令小礼 / 观星礼录 / S3 萌宠手记，10 分钟冷却）
- 神秘商人自动购买（点券 / 金豆豆 / 金币）
- 自动接好友申请（规划中）

### 🌸 公益小红花（已实现 · 2026-09-01 抓包实锤）

> 协议 `ActivityService.Operate{ field1=活动id(2026090901), field2=cmd, field<cmd+99>={} }`，扩展块字段号 = cmd + 99（空子消息，必带，缺则「活动参数错误」）。抓包请求体经 ACE 离线解密逐字节验证。

| 功能 | 状态 | 说明 |
|---|---|---|
| 送出爱心值（cmd=36，f135 空块） | ✅ 已实现 | 捐赠后从背包扣除 1040 爱心值；返回真实错误（如「爱心不足」） |
| 送出公益金（cmd=38，f137 空块） | ✅ 已实现 | 1 元公益助力；附带**公益礼包**化肥(1小时)×2，每日限领 1 次；单账号活动期仅 1 次资格（已做防重标记） |
| 个人爱心值档位领奖（cmd=39，f138 空块） | ✅ 已实现 | 5 档（30/60/90/120/150）；未达成档位返回真实提示，不谎报成功 |
| 分享奖励（cmd=35，f134 空块） | ✅ 已实现 | 每日分享奖励 |
| 全服公益目标进度 | ✅ 已实现 | 全服累计/目标（上限 1000 万元），进度条 + 百分比实时显示 |
| 累计已捐赠爱心值 | ✅ 已实现 | 各档位条目显示「已捐/阈值」，已捐不随领奖减少 |
| 物品名识别 | ✅ 已实现 | 背包识别小红花种子(20883) / 爱心值(1040) |

### 🔧 系统能力
- Web 管理面板（首页 / 好友 / 商城 / 活动 / 分析 / 每日任务 / 设置）
- 自动重连（断线自动恢复）
- 网关心跳保活
- 操作日志（面板实时查看）
- 今日收益统计（金币 / 经验 / 操作次数）
- 离线通知双通道：**MeoW + Bark** 并行推送（掉线 / 重连 / 每日收益日报，设置页两级 tab 独立开关）
- 每日收益定时推送（北京时间指定时刻，改时间当天生效）

## 🚀 部署

### 一键部署（推荐，Rocky Linux 9.6 / Debian / Ubuntu）
root 执行即可。Rocky 9 走 dnf，脚本会安装 Go 1.25 与 Node 22、2G 机自动加 1G swap、编译前后端、装到 `/opt/go-farm-bot`、注册 systemd、放行 3009。

```bash
git clone https://github.com/Aoluis1005/QQ-farm-BOT-GO.git
cd QQ-farm-BOT-GO
bash install.sh
```

部署完成后打开 **`http://<服务器IP>:3009`**（本机示例：`http://103.117.136.113:3009`）。云厂商安全组也要放行 TCP 3009。

**后续更新**同样是这两步，脚本会自己先停服务再覆盖、装完自动拉起，不用手动 `systemctl stop`：

```bash
cd QQ-farm-BOT-GO
git pull
sudo bash install.sh
```

更新完想确认线上跑的是哪一版，返回里的 `version` 就是 git 短哈希：

```bash
curl -s http://127.0.0.1:3009/api/health
```

> 💡 `install.sh` 每次都会**自动重新构建前端**（检测并自动安装 Node → `npm ci` + `vite build`）后再编译后端，保证页面主题/样式始终完整。请勿删除或替换 `web/dist`，也不要手动放置旧版可执行文件——否则可能导致页面白屏、无任何 UI 样式。

### Docker 部署（可选）
仓库自带 `Dockerfile` + `docker-compose.yml`，适合没有 systemd 的环境（LXC 容器 / 群晖 / 其他主机），与 install.sh 互不影响：

```bash
# 方式一：docker compose（推荐）
docker compose up -d --build

# 方式二：纯 docker（可选指定版本号，默认 dev）
docker build --build-arg VERSION=$(git rev-parse --short HEAD) -t go-farm-bot .
docker run -d --name go-farm-bot -p 3009:3009 -e ADMIN_PORT=3009 \
  -v qq-farm-bot-data:/root/.qq-farm-bot go-farm-bot
```

- 管理页面：`http://<服务器IP>:3009`
- **数据持久化**：账号/配置/日志全部在容器卷 `qq-farm-bot-data`（挂载到 `/root/.qq-farm-bot`），重建容器不丢数据
- **前端内嵌**：`web/dist` 已随镜像打进二进制，无需额外静态服务器
- **资源目录**：`game-config`（素材配置）与 `yyb-resource`（YYB 扫码）已随镜像复制，无需手动放置
- 升级：`docker compose up -d --build` 重新构建即可；想换版本号先 `FARM_VERSION=<hash> docker compose build`
- 时区无关：日志固定输出北京时间（UTC+8）

## 📦 快速开始

### 编译
```bash
# Go 1.20+
CGO_ENABLED=0 go build -o go-farm-bot .
```

### 运行
```bash
./go-farm-bot
# 默认监听 :3009
```

### 首次配置
1. 浏览器打开 `http://<服务器IP>:3009`
2. 进入管理面板 → **设置** → 设置管理密码（`/api/admin/setup`）
3. 添加账号 → 微信扫码 / 授权登录（YYB 渠道 = wx 平台）
4. 打开自动化开关，Bot 即开始挂机

### systemd 部署（推荐，裸机）
```ini
# /etc/systemd/system/go-farm-bot.service
[Unit]
Description=QQ Farm Bot (Go)
After=network.target

[Service]
WorkingDirectory=/opt/go-farm-bot
ExecStart=/opt/go-farm-bot/go-farm-bot
Environment=ADMIN_PORT=3009
Restart=on-failure

[Install]
WantedBy=multi-user.target
```
```bash
sudo systemctl enable --now go-farm-bot
```

## 📁 项目结构
```
.
├── main.go            # 入口 + 路由注册
├── gw/                # 网关连接（WebSocket / 心跳 / 重连 / 推送）
├── proto/             # Protobuf 编解码（对齐 Node proto/*.proto）
├── automation.go      # 自动化调度（种植 / 好友 / 任务循环）
├── task_auto.go       # 每日任务自动领取
├── activity_api.go    # 活动接口（雨落成诗完全实现 / 公益小红花已实现）
├── shop_api.go        # 商城（种子 / 宠物 / 道具）
├── home_api.go        # 首页数据（资料卡 / 今日收益 / 日志）
├── stats.go           # 今日收益统计
└── web/               # Vue3 前端源码 + dist（embed 进二进制）
```

## ⚙️ 配置说明
- **账号配置**：管理面板逐账号设置（自动化开关 / 种植策略 / 偷菜黑名单 / 间隔）
- **自动化开关**：农场 / 好友 / 每日任务 / 化肥 / 神秘商人 / 卖果实 独立控制
- **种植策略**：`bag_priority`（背包优先）/ `level` / `max_exp` / `max_fert_exp` / `max_profit` / `max_fert_profit` / `preferred`
- **活动种子排除**：活动种子（如星语铃花）自动排除，不会误买导致种植卡死

## ❓ 常见问题

**Q：管理面板登录提示"请输入密码"？**
首次访问先到「设置」页面设置密码（`/api/admin/setup`），之后用该密码登录。

**Q：首页 / 背包 / 好友无数据？**
检查账号是否已连接（首页「连接状态」）。WebSocket 网关 `platform` 只接受 `qq` / `wx`（YYB 扫码请填 `wx`）。

**Q：种植一直失败？**
检查金币是否足够；活动种子已自动排除；如仍失败请提 Issue 附上操作日志。

**Q：如何查看运行日志？**
管理面板「日志」页实时查看；systemd 部署可用 `journalctl -u go-farm-bot -f`。

## ⚠️ 免责声明
- 本仓库**不含**任何逆向工具、解包产物或敏感凭据（openid / 加密材料），请勿提交此类文件
- 仅用于学习与个人自动化研究，请遵守游戏用户协议
- 如有问题请以 [Issue](https://github.com/Aoluis1005/QQ-farm-BOT-GO/issues) 反馈
- 本项目完全免费 · 开源共享 · 拒绝一切收费

## 📝 相关项目
- [Node 版（原版）](https://github.com/Aoluis1005/qq-farm-bot) —— 本项目的协议参考与功能对照

---

> ⭐ 如果这个项目对你有帮助，欢迎 [Star](https://github.com/Aoluis1005/QQ-farm-BOT-GO) 支持，也欢迎提 Issue / PR 一起完善！
